# אפליקציית משחקי מילים בעברית: הוראות ל-Claude Code

> הקובץ הזה נקרא אוטומטית בתחילת כל סשן. הוא מחליף את ה-Custom Instructions של הפרויקט ב-claude.ai.
> ההיסטוריה המלאה, ההחלטות וההיגיון שמאחוריהן נמצאים ב-`docs/HANDOFF.md`. **קרא אותו לפני כל עבודה משמעותית.**

## מה בונים
אפליקציה אחת שמאחדת שלושה משחקי מילים בעברית (RTL) מאחורי מסך ראשי משותף ("Hub"):

| # | משחק | מה עושים | קוד | מפרט |
|---|------|-----------|-----|-------|
| 1 | **פיצוח מילים** (Word Crack) | מנחשים מילה סודית לפי רמזים של מילה אחת, 2 דקות מול השעון | `games/word-crack/word-crack-solo.html` (HTML + Vanilla JS) | `games/word-crack/word-crack-game-spec.md` |
| 2 | **PUZZLIT: חידות ויזואליות** | קלף ויזואלי שמייצג ביטוי, פתגם, מקום או סלנג | `games/puzzlit/PuzzlitApp.jsx` (React + Tailwind + lucide-react) | `games/puzzlit/PUZZLIT_BUILD_PROMPT.md` |
| 3 | **שרשרת** (Chain) | שרשרת צירופי מילים: רואים רק את האות הראשונה של כל מילה | `games/chain/ChainItHebrew.jsx` (React + Tailwind + lucide-react) | `games/chain/LovableUpdate_ChainGame.md` |

**שים לב:** ההוראות המקוריות בפרויקט אמרו ש"כל משחק הוא HTML יחיד ב-Vanilla JS". זה נכון רק לפיצוח מילים. שני המשחקים האחרים הם קומפוננטות React.

## המטרה: שכבת ה-Hub
- **תפריט ראשי:** שלושת המשחקים מוצגים כקוביות (כמו הקובייה של פיצוח מילים היום), והמשתמש בוחר משחק.
- **ניקוד הוליסטי:** הנקודות מכל המשחקים מצטברות לניקוד כולל אחד למשתמש. לא ניקוד נפרד לכל משחק.
- **טבלת ליגה (Leaderboard):** כל המשתמשים מדורגים לפי הניקוד הכולל.
- **משתמשים ללא הרשמה:** מי שלא נרשם עם אימייל ושם משתמש מקבל שם גנרי בפורמט `user6754236783`.
- **מסך הסבר בכניסה לכל משחק:** מסך "איך משחקים" עם כפתור "הבנתי" (הדפוס של `startScreen` בפיצוח מילים).
- **כפתור הסבר חוזר:** כפתור קבוע בחלק העליון, בכל שלב במשחק, שפותח שוב את מסך ההסבר **בלי לאפס את המשחק הפעיל** (ואם יש טיימר, הוא מושהה בזמן שהמסך פתוח, כמו ב-modal היציאה של פיצוח מילים).
- **עיצוב אחיד** בין ה-Hub למשחקים (פונטים, מבנה כפתורים), **תוך שמירה על הזהות הייחודית של כל משחק בפנים**.
- **אסור לשבור את הלוגיקה הפנימית של אף משחק.** ראה "חוקים שאסור לשבור" למטה.

## החלטה פתוחה שחוסמת את העבודה: ארכיטקטורת האחסון
ניקוד הוליסטי וליגה בין משתמשים דורשים **אחסון משותף** (localStorage פרטי לכל מכשיר ולא מספיק). **לפני שבונים את שכבת המשתמשים והליגה, שאל את עומר ואשר איתו את הכיוון.**

המלצה, לאישורו:
- ה-spec של השרשרת (`LovableUpdate_ChainGame.md`) כבר מניח **Supabase** (`auth.users`, RLS, `public.is_admin()`) ואפליקציית PUZZLIT קיימת ב-Lovable עם פאנל `/admin`. לכן הכיוון הטבעי הוא **React (Vite או Next.js) + Tailwind + Supabase**.
- פיצוח מילים יעבור ל-React כקומפוננטה, **בלי שינוי בחוקים, בניקוד ובמאגר**. המאגר `WORDS_DEFAULT` יעבור לקובץ נתונים או לטבלה.
- משתמשים אנונימיים: Supabase Anonymous Sign-In, עם `display_name` ברירת מחדל `user` + 10 ספרות.
- טבלאות משותפות מוצעות: `profiles` (id, display_name, is_anonymous), `score_events` (user_id, game, points, created_at), ו-view לליגה שמסכם את `score_events`.

## ניקוד הוליסטי: בעיה שצריך לפתור
לכל משחק יש סקאלה אחרת:
- פיצוח מילים: 1 עד 5 נקודות למילה, בדרך כלל כמה עשרות לסיבוב.
- PUZZLIT: 100 עד 1200 XP לחידה.
- שרשרת: **זמן** (נמוך יותר טוב יותר). אין בכלל נקודות.

צריך נוסחת המרה לנקודות אחידות. **הצע לעומר 2-3 חלופות ותן לו לבחור. אל תחליט לבד.**

## חוקים שאסור לשבור (לכל משחק)
### פיצוח מילים
- 120 שניות. הטיימר **נעצר** בניחוש נכון, מציג "✓ מילה" במשך 500ms וממשיך.
- ניקוד: רמז 1 = 5 נקודות, 2 = 4, 3 = 3, 4 = 2, 5 = 1. דילוג = 0.
- ניחוש שגוי חושף את הרמז הבא (עד 5).
- 449 מילים, 5 רמזים לכל מילה, מהספציפי לכללי. כל רמז הוא מילה בודדת ואף רמז לא מכיל את שורש התשובה.
- קבלת תשובה: התאמה מדויקת, רשימת `accepts`, או רבים אוטומטי (ים/ות, ה→ות).
- מודל יציאה משהה את הטיימר. מסך סיום עם דוח מלא לכל מילה.
- מסך ניהול רמזים: נעול לאימייל `omerzaguril@gmail.com`, override ב-localStorage (`wordCrackOverrides_v1`), ייצוא של כל המאגר.

### PUZZLIT
- נרמול תשובה: הסרת ניקוד, פיסוק ורווחים, ו-lowercase. Levenshtein ≤ `fuzzyTolerance` נחשב נכון. בקוד: עד tolerance+2 מציג "כמעט!" (ב-spec כתוב +1, ראה `docs/HANDOFF.md`).
- כל רמז מוריד 20% (`0.8^hints`). מספר הרמזים המותר תלוי ברמה.
- חידה יומית דטרמיניסטית לפי מספר הימים מאז epoch.
- מסך פתרון עם הסבר צעד אחר צעד שמופיע בהדרגה.

### שרשרת
- **אורך המילה לעולם לא נחשף**: לא משבצות, לא מונה אותיות, ורוחב הגלולות אחיד (`min-w-[130px]`).
- הקלט נכתב **בתוך אותה תיבה, אחרי הקידומת הידועה** (טקסט קבוע), ולא בתיבה ריקה נפרדת.
- רמז = חשיפת האות הבאה + 3 שניות. אין הגבלה על מספר הרמזים.
- ניחוש שגוי: התיבה מתנקה והמילה המלאה שנוסתה מוצגת כצ'יפ אדום עם קו חוצה.
- `word` = הצורה הבסיסית שמקלידים. `displayWord` = צורת הסמיכות שמוצגת אחרי פתרון (מדינה→מדינת). `link` = הכיתוב שמוצג אחרי פתרון.
- ה-spec מגדיר שרשרת אחת ליום (כמו וורדל) עם `scheduled_date` ונעילה אחרי משחק אחד. הקוד הנוכחי עדיין מציג בורר של 17 שרשראות. ראה `docs/HANDOFF.md`.

## עקרונות עבודה
- כשעובדים על משחק ספציפי, נוגעים רק בקבצים שלו. כשעובדים על ה-Hub, קוראים את שלושת המשחקים.
- כל UI ב-`dir="rtl"`, ובודקים RTL בפועל ולא רק מסמכים אותו.
- תוכן בעברית בלבד (אותיות עבריות, בלי לטינית ובלי ספרות במילות משחק).
- לפני שינוי שעלול לשבור חוק מהרשימה למעלה: עצור ושאל.
- עומר מעדיף הסברים קצרים וברורים ומעט צעדים ידניים.
